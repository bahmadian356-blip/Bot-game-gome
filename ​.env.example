# BEHI PLAY - Dependency manifest
# This project runs on Node.js (see package.json for exact npm packages + versions).
# This file is provided as a plain-text reference list of every dependency used,
# as requested. Install everything with: npm install

express==4.19.2            # Web server / REST API / static file hosting
socket.io==4.7.5           # Realtime bridge (server-side, wraps Supabase Realtime events to clients)
node-telegram-bot-api==0.66.0  # Telegram Bot API client (polling/webhook, commands, keyboards)
@supabase/supabase-js==2.45.4  # Supabase client (Database + Realtime + Auth)
dotenv==16.4.5              # Loads environment variables from .env
cors==2.8.5                 # CORS handling for the Mini App <-> API calls

# Runtime
node>=18.0.0
